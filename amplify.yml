version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - corepack enable
            - pnpm install --frozen-lockfile
        build:
          commands:
            - pnpm run build
        postBuild:
          commands:
            - echo "NUXT_APP_ENV=$NUXT_APP_ENV" >> $CODEBUILD_SRC_DIR/.output/public/.env
            - echo "NUXT_API_BASE=$NUXT_API_BASE" >> $CODEBUILD_SRC_DIR/.output/public/.env
            - echo "NUXT_OPENAI_API_KEY=$NUXT_OPENAI_API_KEY" >> $CODEBUILD_SRC_DIR/.output/public/.env
      artifacts:
        baseDirectory: .output/public
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
    appRoot: .
